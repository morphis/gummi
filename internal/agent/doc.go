// Package agent defines the adapter abstraction over concrete coding
// agents (Copilot SDK first, opencode later): sessions, event streams,
// interrupts, and per-session environment.
package agent
