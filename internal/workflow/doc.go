// Package workflow implements gummi's single fixed state machine: the
// stages a feature moves through, their legal transitions, skip flags,
// and rerun semantics. The workflow is compiled in, never configured.
package workflow
